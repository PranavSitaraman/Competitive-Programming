#pragma once
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<ctype.h>
#include<math.h>
#include<time.h>
#include<iostream>
#include<algorithm>
#define size3 2500
#define min(x, y) (((x) < (y)) ? ( x) : (y))
#define max(x, y) (((x) > (y)) ? ( x) : (y))
void usacorectpasturerun(void);